<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/space_dark_purple</item>
        <item name="colorPrimaryDark">@color/space_deep_blue</item>
        <item name="colorAccent">@color/neon_blue</item>

        <!-- Customize your theme here. -->
        <item name="android:windowBackground">@color/space_deep_blue</item>
        <item name="android:textColorPrimary">@color/white</item>
        <item name="drawerArrowStyle">@style/DrawerArrowStyle</item>
    </style>

    <style name="DrawerArrowStyle" parent="@style/Widget.AppCompat.DrawerArrowToggle">
        <item name="spinBars">true</item>
        <item name="color">@color/white</item>
    </style>

    <style name="FullscreenTheme" parent="Theme.AppCompat.NoActionBar" />

</resources>
